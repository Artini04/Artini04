---
interface Props {
	class: string

	title: string
	subtitle?: string
}

const { title, subtitle = "//", class: classNames } = Astro.props
---

<div class:list={["skillset-group", ...classNames.split(" ")]}>
	<div class="skillset-group-heading">{title} {subtitle}</div>
	<div class="skillset-group-list"></div>
</div>
